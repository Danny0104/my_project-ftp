# Railway config for the optional Socket.IO chat service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "realtime/chat-server/Dockerfile"
watchPatterns = ["realtime/chat-server/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
